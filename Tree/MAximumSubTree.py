import sys
def recusive(node,flag,max_size,min_value,max_value):
    if node is None:
        flag[0]=True
        return 0
    
def mySolution(node):
    flag=[True]
    max_size=[0]
    min_value=[sys.maxsize]
    max_valeu=[-sys.maxsize-1]
    return max_size[0]

